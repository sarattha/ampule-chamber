apiVersion: v1
kind: Service
metadata:
  name: {{ include "ampule-chamber.fullname" . }}
  labels:
    {{- include "ampule-chamber.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ampule-chamber.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
